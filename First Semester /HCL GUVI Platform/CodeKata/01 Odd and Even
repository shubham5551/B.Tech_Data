#include <stdio.h>

int main() 
{
    int N, M;

    // Input
    scanf("%d %d", &N, &M);
    int sum = N + M;
    if(sum % 2 == 0) // Checking odd or even
    {
        printf("even");
    } 
    else 
    {
        printf("odd");
    }

    return 0;
}
